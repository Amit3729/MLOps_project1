1. Create the project template by exucuting the template.py
2. Write the code on setup.py and pyproject.toml file to import local pacakage
>> There is more about 'setup.py' and 'pyproject.toml' ay course.txt
3. Create virtual Env, activate it and install requriments from requirements.txt 
    conda create -n env_name
    conda activate env_name
    and 'pip install -r requirements.txt' to add required modules
4. 'pip list' to look local pacakage installed

------------MongoDB setup------------
5. Signup to MongoDB atlas and create new project
6.create cluster and deployment
7.setup nad create DB user
8.Get the network access to public so that we can access it from anywhere
9.'get connection string' driver: python , version:3.12 
>> copy and save 
10.create folder 'notebook'
>> create file'mongodb_demo.ipynb'
>>select kernal>pythonkernal>
11.dataset added to notebook
12.push data to mongodb_demo
13. go to mongodb atlas >>database>>browse collection>>see you data in Key-value format

------logging,expecton and notebooks-------
14. Write logger file and test it on demo.py
15. Write expection file and test it
16. EDA and feature Engineering notebook added.

---------Data Ingestion---------
17. Before we work on 'Data Ingestion' In components
>> Declear variables within constants.__init__.py file..
>> add code to configuration mongo_db_Connetion.py file and define the func for mongobd connection
>> Inside the 'data_acess', added code to proj1_data.py that will use mongo_bf_connections.py
>> to connect with DB, fetch data in key-value format and transform that to df 
>> add code to entity--config_entity.py file till DataIngestionConfig class
>> add code to entity.artifact_entity.py file till DataIngestionArtifacr class
>> add code to components.data_ingestion.py file 
>> add code to training pipeline
>> run demp.py(set mongobf connection url first)

For setup connection url
$env:MONGODB_URL="mongodb+srv://<username>:<password> or for bash>>
export MONGODB_URL="your_connection_string" 

for check: echo $MONGODB_URL

Don;t forget to add 'artifact' dir to .gitignore file

------------------Data Validation , Data Transformation and Model Trainer-------------

18. Complete the wrok in utils.main_utils.py and config.schema.yaml file(add entire info about dataset for data validation setp)
19. Now work on the "data Validation" component the way we did in step 17 for Data Ingesgtion.
20. Now work on the "data Transformation" component the way we did like as above step(add estimator.py to entity folder)
21. Now work ont the "Model Trainer" component like above setp. (Add class to class estimator.py in entity folder)

23. Before moving to next component of model Evulation, some AWS services setup is needed.
    - Login to AWS console.
    - Kepep region set as -us-east-1
    - Go to IAM >> Create new user(name: name_of project)
    - Attach policy >> select AdministratorAcess >> next >> Create user
    - Go to the user >> security Credentials >> Acess keys >> Create access key
    - Select CLI >> agree to condition >> next >> create Acess key >> Download scv file
    - Set env variables with above csv values using beow method:
    -------------------------------------------------------------------
        Set env var from terminal:
        export AWS_ACESS_KEY_ID='AWS_ACCESS_KEY_ID'
        export AWS SECRET_ACCESS_KEY='AWS_SECRET_ACCESS_KEY'
        >> check env var from terminal:
        echo $AWS_ACCESS_KEY_ID


    - Now add the access key, secret key, region name to cnstants.__init__.py
    - Add code to src.configuration.aws_connection.py file(to work with AWS s3 service)
    - Ensure below info in consatants.__init__.py file:
        MODEL_EVULATION_CHNAGED_THRESHOLD_SCORE: float = 0.02
        MODEL_BUCKET_ANME = "YOUR BUCKET NAME"
        MODEL_PUSHER_S3_KEY = 'model-registry'
    - Go to s3 services ..create bucket..region us-east-1 ..general purpose
    >>bucket name:'my-model-proj7'..>> uncheck block section adn acknowlwdge.
    >>Hit create bucket
    - Now inside 'src.aws_storage' code needs to be added for the configuration needed to pull and push model from AWS s3 bucket.
    -Inside "entity" dir we will have an 's3_estimator.py' file contaiing all the func to pull/push data form s3 bucket.

24. Now we will start out work on model Evulation and model pusher component.

    







